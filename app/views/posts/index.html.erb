<% if @posts.any? %>
  <% @posts.each do |post| %>
    <h2><%= post.title %></h2>
    <p><%= post.description %></p>
    <p><%= image_tag post.image.url, class: 'uploaded-pic' %></p>
    <% end %>
<% else %>
  <h1>No posts yet</h1>
<% end %>

<%= link_to 'New post', new_post_path %>